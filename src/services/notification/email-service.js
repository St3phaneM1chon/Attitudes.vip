// Service d'envoi d'emails (placeholder)
module.exports = {
  sendEmail: async (to, subject, body) => {
    // TODO: Implémenter avec SendGrid
    console.log(`Email envoyé à ${to}: ${subject}`)
    return true
  }
}