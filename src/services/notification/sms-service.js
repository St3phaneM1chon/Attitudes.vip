// Service d'envoi de SMS (placeholder)
module.exports = {
  sendSMS: async (to, message) => {
    // TODO: Implémenter avec Twilio
    console.log(`SMS envoyé à ${to}: ${message}`)
    return true
  }
}